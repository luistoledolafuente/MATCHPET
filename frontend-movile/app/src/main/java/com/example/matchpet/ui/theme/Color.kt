package com.example.matchpet.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales de MatchPet
val PrimaryTeal = Color(0xFF2D5F5D)        // Color teal oscuro (botones, headers)
val PrimaryTealLight = Color(0xFF3A7370)   // Variante más clara
val SecondaryPink = Color(0xFFE89CAC)      // Color rosa (botón Donar)
val SecondaryPinkLight = Color(0xFFF4B5C4) // Variante más clara

// Colores de fondo
val BackgroundLight = Color(0xFFF5F5F5)    // Fondo gris claro
val BackgroundBeige = Color(0xFFFFF8F0)    // Fondo beige claro (campos de texto)
val SurfaceWhite = Color(0xFFFFFFFF)       // Blanco puro

// Colores de texto
val TextPrimary = Color(0xFF1A1A1A)        // Texto principal oscuro
val TextSecondary = Color(0xFF666666)      // Texto secundario gris
val TextPlaceholder = Color(0xFF999999)    // Texto placeholder

// Colores de bordes
val BorderGray = Color(0xFFE0E0E0)         // Bordes grises
val BorderTeal = Color(0xFF2D5F5D)         // Bordes teal (tabs activos)

// Colores para estados
val ErrorRed = Color(0xFFDC3545)
val SuccessGreen = Color(0xFF28A745)

// Dark Theme (opcional para futuro)
val DarkBackground = Color(0xFF1A1A1A)
val DarkSurface = Color(0xFF2D2D2D)